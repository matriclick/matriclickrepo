source 'http://rubygems.org'

gem 'rails', '3.1.0'

# Bundle edge Rails instead:
# gem 'rails',     :git => 'git://github.com/rails/rails.git'

gem 'mysql2'

gem 'devise', :git => "git://github.com/redvel-ltda/devise", :branch => "rails3.1"
gem 'omniauth', '0.2.6'

gem 'nifty-generators'
gem 'awesome_print'

gem 'paperclip', "~> 2.3", :git => "git://github.com/redvel-ltda/paperclip.git"
# Mime Types for swfupload
gem 'mime-types', :require => 'mime/types'
gem 'cancan'
gem 'nested_form', :git => "https://github.com/fxposter/nested_form.git"
gem 'will_paginate', '~> 3.0'
gem 'spreadsheet'

gem 'jquery-rails'
gem 'geocoder'
gem 'client_side_validations'

gem 'sitemap_generator'

gem 'friendly_id'

gem 'rubyzip'

#Gems used in Google Analytics
gem 'garb', :git => "https://github.com/vigetlabs/garb.git"

gem 'whenever', :require => false

# Use unicorn as the web server
# gem 'unicorn'

# Deploy with Capistrano
gem 'capistrano'

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails', "~> 3.1.0"
  gem 'coffee-rails', "~> 3.1.0"
  gem 'uglifier'
end

group :test do
  # Pretty printed test output
  gem 'turn', :require => false
end

group :production do
  gem 'exception_notification'
end

