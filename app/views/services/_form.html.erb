<%= nested_form_for [@supplier_account,@service],:url => supplier_account_services_path(@supplier_account,@service), :validate => true, :html => {:multipart => true} do |f| %>
	<%= render 'form_content', :f => f %>
<% end %>
