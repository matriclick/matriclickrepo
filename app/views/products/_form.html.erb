<%= nested_form_for [@supplier_account,@product],
:url => supplier_account_products_path(@supplier_account,@product),
:validate => true,
:html => {:multipart => true} do |f| %>

	<%= render 'form_content', :f => f %>

<% end %>
